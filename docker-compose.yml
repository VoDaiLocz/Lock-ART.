services:
  auralock:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ./output:/app/output
    restart: unless-stopped
    command: ["auralock", "webui", "--host", "0.0.0.0", "--port", "7860"]
